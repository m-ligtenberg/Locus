{
  "name": "locus",
  "version": "5.0.0",
  "description": "AI-powered travel companion with voice cloning",
  "scripts": {
    "dev": "vercel dev",
    "build:frontend": "cd frontend && npm run build",
    "build:docs": "cd docs-site && npm run build",
    "build:all": "npm run build:frontend && npm run build:docs",
    "start": "vercel --prod",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "openai": "^4.20.1"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "@vercel/node": "^2.3.0"
  }
}
